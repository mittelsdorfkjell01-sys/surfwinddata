# Keep the build context small and never bake secrets or local state into the image.
.git
.gitignore
.venv
**/__pycache__
*.py[cod]
.pytest_cache
.env
.env.*
!.env.example

# Frontend is deployed separately (static host) — not part of the API image.
frontend

# Local data / backups / downloaded ERA5 extracts.
data
backups
screens

# Docs / dev-only
tests
Konzept und Prompts
*.md
docker-compose.yml
docker-compose.prod.yml
