"""Public visitor accounts: sign-up, session, favourites, spot proposals.

This package is served by **every** deployment (unlike the admin back office),
because the account area lives on the public site (surfwinddata.com). See
:mod:`app.account.security` for the app-scoped session token.
"""
